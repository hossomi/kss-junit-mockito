<configuration debug="false">

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%-40(%d{HH:mm:ss.SSS} | %-5level | %-60logger{60}) -- %msg%n</pattern>
        </encoder>
    </appender>

    <root level="${level.default}">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>